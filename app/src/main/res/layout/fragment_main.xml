<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:padding="@dimen/activity_vertical_margin"
        app:srcCompat="@drawable/ic_wign_logo"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/Welcome_msg"
        android:textSize="@dimen/Welcome_msg"
        android:textColor="@color/WignGreen"
        android:layout_margin="40dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_marginEnd="@dimen/activity_horizontal_margin"
            android:layout_height="wrap_content"
            android:text="@string/search"
            android:textColor="@color/colorText"/>

        <!-- AutoCompleteTextView later! -->
        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/search_sign"
            android:inputType="text"
            android:padding="@dimen/btn_padding"
            android:background="@color/colorBG_Light"
            android:textColor="@color/colorBlack"/>

    </LinearLayout>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btn_find_sign"
        android:text="@string/btn_find_sign"
        android:background="@color/WignGreen"
        android:textColor="@color/colorBlack"
        android:padding="@dimen/btn_padding"
        android:layout_margin="@dimen/btn_padding"
        android:layout_gravity="center"/>

</LinearLayout>
