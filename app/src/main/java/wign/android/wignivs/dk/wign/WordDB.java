package wign.android.wignivs.dk.wign;

import android.content.Context;

import io.realm.Realm;

/**
 * Singleton! Not needed?
 * Created by Troels on 29/03/2017.
 */

/*
public class WordDB {
    private Realm realm;
    private static volatile WordDB wordDB;

    public static WordDB getInstance(Context context) {
        if (wordDB == null) {
            synchronized (WordDB.class) {
                if (wordDB == null) {
                    wordDB = new WordDB();
                }
            }
        }
        return wordDB;
    }
}
*/
